# Web Framework
fastapi==0.115.5
uvicorn[standard]==0.32.1

# Settings
pydantic-settings==2.6.1

# Database
sqlalchemy==2.0.36
psycopg2-binary==2.9.10

# LINE Bot SDK v3
line-bot-sdk==3.13.0

# Gemini
google-generativeai==0.8.3

# RAG 向量運算（app/services/rag.py 直接 import numpy 做 cosine
# similarity；過去一直靠 pgvector 套件順帶把 numpy 裝進來，從沒有
# 明確宣告過這個依賴——拿掉 pgvector 之後這裡就直接斷了，讓部署因為
# ModuleNotFoundError: No module named 'numpy' 在 healthcheck 階段
# 失敗。明確宣告，不要再依賴別的套件順便帶進來。
numpy==2.4.6

# Scheduler
apscheduler==3.10.4

# Utilities
python-dotenv==1.0.1
python-multipart==0.0.18
networkx==3.4.2

# Rate limiting（系統沒有登入驗證，用來擋掉短時間內狂打同一 IP 的濫用）
slowapi==0.1.10

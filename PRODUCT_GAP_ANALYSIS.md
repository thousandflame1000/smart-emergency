# 產品落差分析：本專案 vs. Palantir 級系統

一句話定位：Palantir Foundry/AIP、One Concern、WebEOC 是**企業級資料整合與決策平台**，服務對象是政府/跨機構指揮中心，投入是數百人年工程 + 專屬資料合約。本專案是**單一鄰里社群的物資調度原型**，範圍、預算、團隊規模完全不同量級。以下比較不是為了證明「做得到」，是為了誠實標出差距、決定哪些值得補、哪些不該碰。

## 能力對照表

| 能力構面 | Palantir 級系統 | 本專案現況 | 差距 |
|---|---|---|---|
| 資料本體/實體建模 | 正式 Ontology：物件+屬性+連結+行動，人機皆可讀 | 5 張 SQLAlchemy 表，關聯靠外鍵，無語意層 | 中——夠用但不可擴充到新實體類型 |
| 派遣/資源分配演算法 | 專屬求解器 + 人工校準的運籌模型 | 匈牙利演算法批次最佳指派（`app/services/dispatch.py`），單一資源類型、單一約束維度，距離改用真實花東路網（`app/services/road_network.py`）而非直線距離 | 中——演算法核心正確，但只解一種問題（1對1指派），不含時間窗/多資源打包；路網目前只涵蓋花東走廊，非全台 |
| 災害情境建模 | 物理數位分身（感測器網路 + 即時校正） | 參數化統計模型（Holland 風場 + Kaplan-DeMaria 衰減 + 地形衰減）驅動的**可調參數 sandbox**（強度/動員量/人口規模三個真實輸入變數，非固定劇本），無感測器、無即時校正 | 大——本專案能「假設推演」（what-if），但不是「持續與現實同步的分身」；沒有數字分身該有的即時資料回饋迴路 |
| 資料整合 | 跨機構即時資料管線（多來源 ETL、資料血緣追蹤） | 單一資料庫，手動 seed script | 大——沒有外部資料源整合能力 |
| 即時性 | 秒級資料流、事件驅動架構 | 30 分鐘排程輪詢 | 大——不是即時系統，是批次系統 |
| 身份/權限治理 | 細粒度 RBAC、稽核日誌 | 一組共用密碼（`DEMO_PASSWORD`），無角色分級 | 大——目前是「有沒有密碼」的二元閘，不是權限系統 |
| 決策可解釋性 | Ontology 層面人機共讀 | 候選分數分解 + hover tooltip（`preview_candidates`） | 小——概念一致，實作規模不同但方向正確 |
| 部署規模與可用性 | 多資料中心、SLA 保證 | 單一 Railway 容器，無備援 | 大——單點故障，Railway 掛了系統就掛了 |
| 驗證與試辦數據 | 多年真實案例校準 | 零真實試辦資料（合成人口 + 一位真實使用者） | 大——這是計畫書已承認的硬天花板，非本輪能解 |

## 三週內值得補的（低成本、有真實提升）

1. **Layer 2（資源點）也納入批次指派**——目前只有 Layer 1（個人物資）用匈牙利演算法，資源點仍是逐筆貪婪比對。資源點非稀缺，理論上不需要，但如果之後要支援「同一資源點有容量上限」，這裡會是第一個要動的地方。**先不做**，目前資源點無容量欄位，做了也用不到。
2. **後台角色分級**（志工只能看自己名下任務，管理員看全部）——比密碼閘更接近真正的權限治理，但需要登入態基礎建設，超出決賽前範圍，列為賽後第一優先。
3. **決策稽核日誌**（誰在什麼時候確認/否決了哪筆派遣）——`confirm_dispatch`/`decline_suggestion` 目前不記錄操作者，這是 Ontology「行動可追溯」精神裡最便宜就能做到的一塊。

## 不該現在做、也不該對評審宣稱做得到的

- 物理數位分身、感測器網路、跨機構資料管線：本專案規模不具備，做了也是空殼展示，等於自己挖坑給自己跳。
- 即時事件驅動架構：目前 30 分鐘排程對社區規模已足夠，改成事件驅動是工程投入換不到實際效益的方向。
- 多資料中心/SLA：跟決賽展演目標無關，是正式營運才需要考慮的問題。

## 一個誠實的發現：真實地理會打破「脆弱度必勝」的簡化敘事

把情境模擬從人口密集的台中改成花東縱谷真實鄉鎮（相隔動輒 15–40 公里）後，實測發現：都會區示範資料下「王秀霞脆弱度最高、一定贏得稀缺資源」的敘事，在真實鄉村地理距離下**不成立**——`dispatch.py` 的 `URGENCY_MAX_KM`（依緊急度設的距離上限，最緊急僅 2 公里）是照都會社區「志工徒步/機車可及範圍」校準的，套進真實道路網一量，鄉鎮之間的距離遠遠超出這個範圍，脆弱度再高也贏不了「根本不在資源可達範圍內」這個硬限制。

這不是 bug，是把假資料換成真實地理座標後才會暴露的真實限制，直接印證了原本合成資料掩蓋掉的問題。目前的因應：資源動員地點改用「涵蓋最多真實通報者的地理群集」而非任意點（`app/services/scenario.py` 的 `_coverage` 邏輯），讓稀缺資源至少能在有意義的競爭中被指派，但**不會、也不該**為了讓特定角色贏而扭曲演算法——評審問到「這樣脆弱度優先權還有意義嗎」時，誠實的答案是：意義在「同一可達範圍內」脆弱度決定優先權，範圍本身則是地理/資源動員能力的真實限制，這正是為什麼這個系統需要城鄉不同的距離校準參數（目前尚未做，列入賽後項目）。

## 結論

核心演算法（脆弱度評分、匈牙利批次指派、颱風風場模型）在**方法論**層面站得住腳，跟 Palantir 級系統的差距主要在**規模、資料整合廣度、即時性、權限治理**——這些是工程投入量級的差距，不是設計思路錯誤。誠實的定位是：「一個方法論正確、規模對應決賽/單一社區場景的原型」，不是「縮小版 Palantir」。回答評審時用前者的敘事，後者站不住腳、會被拆穿。
